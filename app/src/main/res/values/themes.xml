<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.AndroidSilencerApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
    </style>

    <style name="Theme.AndroidSilencerApp" parent="Base.Theme.AndroidSilencerApp" />

    <style name="Theme.AndroidSilencerApp.NoActionBar" parent="Theme.AndroidSilencerApp">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
</resources>